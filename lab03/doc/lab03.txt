Основна частина:
- опис роботи основної функції:
 площа квадрата: S = a * a , де a - сторона квадарата;
 периметр Р=а*4
- перелік вхідних даних:
- а- сторона, позитивне дійсне число (float);
1. Объявление змінних:
float сторона-a, площа-area, периметр- perimeter
2. Введення довжини сторони квадрата:
printf("Введіть довжину сторони квадрата (a): ");
scanf("%f", &a);
3. Обчислення площі та периметра квадрата:
area = a * a;
perimeter = 4 * a;
4. Вивід результатів:
printf("Площа квадрата: %.2f\n", area);
printf("Периметр квадрата: %.2f\n", perimeter);
5. Повернення значення:
return 0;

Код програм:
#include <stdio.h>
int main() {
    // Оголошення змінних
    float a, area, perimeter;
    // Введення довжини сторони квадрата
    printf("Введіть довжину сторони квадрата (a): ");
    scanf("%f", &a);
    // Обчислення площі квадрата
    area = a * a;
    // Обчислення периметра квадрата
    perimeter = 4 * a;
    // Виведення результатів
    printf("Площа квадрата: %.2f\n", area);
    printf("Периметр квадрата: %.2f\n", perimeter);
    return 0;
}

У даному коді точкою входу є функція main(). Точка входу визначає місце, звідки розпочинається виконання програми. 
Місце знаходження: Точка входу розташована на початку програми, визначається ключовим словом int main(). 
Усе, що розташоване між відкриваючою фігурною дужкою { та закриваючою }, є тілом функції main().
Одність: В стандарті мови програмування C, існує вимога наявності однієї і тільки однієї функції main() в програмі. 
Інші функції можуть бути визначені, але вони викликатимуться з main() або з інших функцій, що викликаються з main().
Призначення: Функція main() визначає вхідний пункт для виконання програми.
Коли програма запускається, виконання розпочинається саме з цієї функції.

Виконуєм зупинку посередині виконання програми за допомогою breakpoints. Приклад зміни стану програми “на льоту”
Точки зупинки 
- break set --name main
- строка в 12 (Обчислення площі квадрата)
Спочатку вводимо дожину 5, потім, на другій зупинці змінюєм значення на 7,5. 
Перевіряємо обчислювання площі з новим значенням вводячі print area

Фрагмент з отладкою:

(lldb) break set --name main
Breakpoint 1: where = main`main + 15 at main.c:8:5, address = 0x000000000000115f
(lldb) b 12
Breakpoint 2: where = main`main + 47 at main.c:12:12, address = 0x000000000000117f
(lldb) r
Process 7001 launched: '/home/sokolov/Документы/lab03/dist/main' (x86_64)
Process 7001 stopped
* thread #1, name = 'main', stop reason = breakpoint 1.1
    frame #0: 0x000055555555515f main`main at main.c:8:5
   5   	    float a, area, perimeter;
   6   	
   7   	    // Введення довжини сторони квадрата
-> 8   	    printf("Введіть довжину сторони квадрата (a): ");
   9   	    scanf("%f", &a);
   10  	
   11  	    // Обчислення площі квадрата
(lldb) c
Process 7001 resuming
Введіть довжину сторони квадрата (a): 5
Process 7001 stopped
* thread #1, name = 'main', stop reason = breakpoint 2.1
    frame #0: 0x000055555555517f main`main at main.c:12:12
   9   	    scanf("%f", &a);
   10  	
   11  	    // Обчислення площі квадрата
-> 12  	    area = a * a;
   13  	
   14  	    // Обчислення периметра квадрата
   15  	    perimeter = 4 * a;
(lldb) print a
(float) $0 = 5
(lldb) expr a = 7.5
(float) $1 = 7.5
(lldb) c
Площа квадрата: 56.25
Периметр квадрата: 30.00
Process 7001 exited with status = 0 (0x00000000) 
Process 7001 resuming
(lldb) 

Структура проекту лабораторної роботи:
└── lab03
├── README.txt
├── Makefile
└── src
└── main.c

Висновки: при виконанні лабораторної роботи буди набуті практичні навички створення лінійних програм на мові С, зокрема: визначати типи даних, обчислювати 
математичні вирази, емулювати операцію зведення до ступеню через операцію множення.
